{
  "name": "model-ts",
  "repository": "git@github.com:finkef/model-ts.git",
  "author": "Fabian Finke <finkef@icloud.com>",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "resolutions": {
    "typescript": "4.5.5"
  },
  "scripts": {
    "build": "yarn tsc -b",
    "clean": "yarn tsc -b --clean",
    "release": "yarn build && yarn changeset publish"
  },
  "dependencies": {},
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-eventbridge": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@changesets/cli": "^2.16.0",
    "@types/jest": "^29.5.0",
    "@types/node": "20.11.0",
    "fp-ts": "^2.6.7",
    "io-ts": "^2.2.16",
    "typescript": "~4.5.5"
  }
}
